apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sentiment-agent
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: sentiment-agent
  endpoints:
  - port: metrics
    path: /metrics
    interval: 30s
